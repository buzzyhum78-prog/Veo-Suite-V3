"""Built-in plugins shipped with Veo Suite V3.

These exist so the plugin pipeline is exercised even on a clean
install (no user-installed plugins) and so the plugin API has at
least one in-tree example for future authors to crib from.
"""
